<% rcc = Rocco.new( '.rb', [], {stylesheet: 'https://raw.github.com/jashkenas/docco/master/resources/linear/docco.css'} ) { 
<<EOF
  activate :blog do |blog|
    blog.prefix = "blog"
    # blog.permalink = ":year/:month/:day/:title.html"
    # blog.sources = ":year-:month-:day-:title.html"
    # blog.taglink = "tags/:tag.html"
    # blog.layout = "layout"
    # blog.summary_separator = /(READMORE)/
    # blog.summary_length = 250
    # blog.year_link = ":year.html"
    # blog.month_link = ":year/:month.html"
    # blog.day_link = ":year/:month/:day.html"
    # blog.default_extension = ".markdown"

    blog.tag_template = "tag.html"
    blog.calendar_template = "calendar.html"

    # blog.paginate = true
    # blog.per_page = 10
    # blog.page_link = "page/:num"
  end

  page "/feed.xml", :layout => false
EOF
} %>

<%= rcc.to_html %>

<%= (Rocco.new( 'gamme' ){  File.new('config.rb', 'rb').read}).to_html.to_s  %> 
<%= (Rocco.new( 'beta' ){  File.new('config.rb', 'rb').read}).to_html.to_s  %>  
<%= "done" %>
